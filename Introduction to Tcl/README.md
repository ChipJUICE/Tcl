# 🧠 Introduction to Tcl

This repository provides a step-by-step introduction to the **Tool Command Language (Tcl)** — a simple, powerful scripting language often used in EDA automation, testbench scripting, and rapid tool development.  
Each `.tcl` file covers a fundamental concept in Tcl programming, arranged in a logical learning flow.

---

## 📂 Repository Structure

| File | Topic | Description |
|------|--------|-------------|
| **HelloWorldBasics.tcl** | **Tcl Basics** | Introduces basic syntax, variable assignment, `puts` command, arithmetic operations, and simple output handling. |
| **Lists.tcl** | **Working with Lists** | Covers creation, indexing, iteration, and manipulation of Tcl lists using commands like `list`, `lindex`, `llength`, and `foreach`. |
| **Loops.tcl** | **Control Flow – Loops** | Demonstrates loop constructs (`for`, `while`, and `foreach`) with examples for repetitive operations and nested looping. |
| **Cond_Proc.tcl** | **Conditionals and Procedures** | Explains conditional branching (`if`, `elseif`, `else`) and how to define reusable procedures using `proc`. |
| **Array_String.tcl** | **Arrays and String Operations** | Introduces associative arrays, indexing, string manipulation functions (`string length`, `string compare`, etc.). |
| **Files.tcl** | **File Handling in Tcl** | Demonstrates file I/O operations — opening, reading, writing, and closing files using `open`, `gets`, `puts`, and `close`. |

---

## 🧭 Recommended Flow

```
HelloWorldBasics.tcl → Lists.tcl → Loops.tcl → Cond_Proc.tcl → Array_String.tcl → Files.tcl
```
